# Вартість та масштабування — AI Support Chat Analyzer

## 1. Ціни OpenAI API (актуально на лютий 2026)

| Модель | Input (за 1M токенів) | Output (за 1M токенів) |
|--------|----------------------|------------------------|
| **GPT-4o** | $2.50 | $10.00 |
| **GPT-4o-mini** | $0.15 | $0.60 |

> Джерело: [OpenAI Pricing](https://openai.com/api/pricing/)

---

## 2. Розрахунок вартості для 120 діалогів

### 2.1 Етап генерації (generate.py) — GPT-4o-mini

**Оцінка токенів на 1 діалог:**
| Компонент | Токени |
|-----------|--------|
| System prompt | ~300 |
| User prompt (інструкція + сценарій) | ~150 |
| Відповідь (діалог 6-14 реплік) | ~400 |
| **Всього на 1 діалог** | **~850** |

**Вартість генерації 120 діалогів:**
| Метрика | Значення |
|---------|----------|
| Input токени | 120 × 450 = **54 000** |
| Output токени | 120 × 400 = **48 000** |
| Вартість input | 54K / 1M × $0.15 = **$0.008** |
| Вартість output | 48K / 1M × $0.60 = **$0.029** |
| **Разом генерація** | **~$0.04** |

### 2.2 Етап аналізу (analyze.py) — GPT-4o

**Оцінка токенів на 1 аналіз:**
| Компонент | Токени |
|-----------|--------|
| System prompt | ~200 |
| User prompt (інструкція + діалог) | ~500 |
| Відповідь (JSON з аналізом) | ~100 |
| **Всього на 1 аналіз** | **~800** |

**Вартість аналізу 120 діалогів:**
| Метрика | Значення |
|---------|----------|
| Input токени | 120 × 700 = **84 000** |
| Output токени | 120 × 100 = **12 000** |
| Вартість input | 84K / 1M × $2.50 = **$0.21** |
| Вартість output | 12K / 1M × $10.00 = **$0.12** |
| **Разом аналіз** | **~$0.33** |

### 2.3 Підсумок вартості (120 діалогів)

| Етап | Модель | Вартість |
|------|--------|----------|
| Генерація | GPT-4o-mini | $0.04 |
| Аналіз | GPT-4o | $0.33 |
| **Повний pipeline** | — | **~$0.37** |

> Враховуючи retry-логіку (до 10% повторних запитів), реалістична вартість: **~$0.40**

---

## 3. Порівняння стратегій використання моделей

### Варіант A: GPT-4o-mini для генерації + GPT-4o для аналізу (рекомендовано)
| | Вартість 120 діалогів | Якість генерації | Якість аналізу |
|-|----------------------|-----------------|----------------|
| | **~$0.37** | Висока | Дуже висока |

### Варіант B: GPT-4o для всього
| | Вартість 120 діалогів | Якість генерації | Якість аналізу |
|-|----------------------|-----------------|----------------|
| | **~$3.00** | Дуже висока | Дуже висока |

### Варіант C: GPT-4o-mini для всього
| | Вартість 120 діалогів | Якість генерації | Якість аналізу |
|-|----------------------|-----------------|----------------|
| | **~$0.08** | Висока | Середня |

**Рекомендація:** Варіант A — оптимальний баланс. Генерація не потребує глибокого аналізу (mini достатньо), але аналіз прихованої незадоволеності потребує потужнішої моделі.

---

## 4. Масштабування

### 4.1 Прогноз вартості при збільшенні обсягу

| Кількість діалогів | Генерація (mini) | Аналіз (4o) | Разом | Час виконання* |
|-------------------|-----------------|-------------|-------|----------------|
| **120** | $0.04 | $0.33 | **$0.37** | ~8 хв |
| **500** | $0.17 | $1.38 | **$1.54** | ~30 хв |
| **1 000** | $0.33 | $2.75 | **$3.08** | ~55 хв |
| **5 000** | $1.67 | $13.75 | **$15.42** | ~4.5 год |
| **10 000** | $3.33 | $27.50 | **$30.83** | ~9 год |

*\* Час при послідовній обробці без паралелізації*

### 4.2 Стратегії оптимізації при масштабуванні

#### a) Async паралелізація
```python
import asyncio
from openai import AsyncOpenAI

# Замість послідовних запитів — паралельні батчі
async def process_batch(chats, batch_size=10):
    client = AsyncOpenAI()
    for i in range(0, len(chats), batch_size):
        batch = chats[i:i+batch_size]
        tasks = [analyze_chat(client, chat) for chat in batch]
        results = await asyncio.gather(*tasks)
```

| Кількість | Послідовно | Паралельно (10) | Прискорення |
|-----------|-----------|----------------|-------------|
| 120 | ~8 хв | ~1.5 хв | 5x |
| 1 000 | ~55 хв | ~10 хв | 5.5x |
| 10 000 | ~9 год | ~1.5 год | 6x |

#### b) OpenAI Batch API (для великих обсягів)
- **50% знижка** на вартість
- Результати доступні протягом 24 годин
- Ідеально для 1000+ діалогів

| Кількість | Звичайний API | Batch API | Економія |
|-----------|-------------|-----------|----------|
| 1 000 | $3.08 | $1.54 | 50% |
| 10 000 | $30.83 | $15.42 | 50% |

#### c) Кешування результатів
- Зберігати згенеровані діалоги локально
- При повторному запуску аналізу — не перегенеровувати
- Зберігати raw API-відповіді для дебагу

#### d) Prompt caching (OpenAI)
- OpenAI автоматично кешує довгі промпти (>1024 токенів)
- Кешовані input-токени коштують **50% дешевше**
- При аналізі 120+ діалогів з однаковим system prompt — значна економія

---

## 5. Rate Limits

### Стандартні ліміти OpenAI (Tier 1)

| Модель | RPM (запити/хв) | TPM (токени/хв) |
|--------|----------------|-----------------|
| GPT-4o | 500 | 30 000 |
| GPT-4o-mini | 500 | 200 000 |

### Стратегія роботи з лімітами

| Обсяг | Стратегія |
|-------|-----------|
| 120 діалогів | Без обмежень — вкладаємось в ліміти послідовно |
| 500 діалогів | Async з batch_size=10, delay 0.5s між батчами |
| 1 000+ | Async з адаптивним batch_size + exponential backoff |
| 10 000+ | OpenAI Batch API + розбиття на кілька запусків |

```python
# Exponential backoff для rate limits
import time

def call_with_retry(func, max_retries=3):
    for attempt in range(max_retries):
        try:
            return func()
        except RateLimitError:
            wait = 2 ** attempt  # 1s, 2s, 4s
            time.sleep(wait)
    raise Exception("Max retries exceeded")
```

---

## 6. Рекомендації

### Для хакатону (120 діалогів)
- Використовувати **Варіант A** (mini + 4o)
- Послідовна обробка з `tqdm` прогрес-баром
- Вартість: **< $0.50**
- Час: **~8 хвилин**

### Для продакшену (1000+ діалогів)
- Async-обробка з паралельними батчами
- OpenAI Batch API для некритичних задач
- Моніторинг витрат через OpenAI Dashboard
- Логування всіх API-відповідей для аудиту

### Для великих масштабів (10 000+)
- Перехід на GPT-4o-mini для аналізу з fine-tuned моделлю
- Batch API + розбиття на добові запуски
- Розглянути self-hosted open-source моделі (Llama, Mistral) для генерації
- Кешування та дедуплікація промптів
